# {{mod_name}}

{{description}}

| | |
|---|---|
| Mod ID | `{{mod_id}}` |
| Minecraft | {{mc_version}} |
| Loader | {{loader_label}} {{loader_version}} |
| Java | {{java_version}}+ |

## Build

```
./gradlew build        # Windows: gradlew.bat build
```

The mod jar is written to `build/libs/`.

## Test in game

```
./gradlew runClient
```

Created with SDAI ARCHIMED Mod Studio.
